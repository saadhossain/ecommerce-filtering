import React from 'react';

const FilterTitle = ({ title }) => {
    return (
        <h6 className='font-semibold border-l-4 border-blue-500 pl-2 my-5'>{title}</h6>
    );
};

export default FilterTitle;